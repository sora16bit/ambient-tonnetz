{
  "name": "tonnet-ambient",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp sitemap.xml dist/sitemap.xml && cp robots.txt dist/robots.txt",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npm run build && wrangler pages deploy dist --project-name ambient-tonnet"
  },
  "devDependencies": {
    "jsdom": "^29.0.2",
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.0.0"
  }
}
